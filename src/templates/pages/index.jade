extends ../layout.jade

block variables
    - pageTitle = "- Pomodoro"
block scripts
    script(src='/pomodoro.js')
block content
    section
        div(id="timer")
        button(id="pomodoro-trigger") Pomodoro!
        button(id="5-min-trigger") 5-min-interval
